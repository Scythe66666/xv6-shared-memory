#include "types.h"
#include "stat.h"
#include "user.h"
#include "fs.h"
#include "shm.h"

int main(int argc, char* argv)
{
    int row_size = atoi(argv[0]); 
    int col_size = atoi(argv[1]); 
    int row_start = atoi(argv[2]);
    int col_start = atoi(argv[3]);
    int row_end = atoi(argv[4]);
    int col_end = atoi(argv[5]);
    
    
}
